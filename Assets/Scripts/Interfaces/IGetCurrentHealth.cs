using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IGetCurrentHealth
{
    // Start is called before the first frame update
    int GetCurrentHealth();

}
